# ProtoPedia API v2 Client - Test Environment Variables
#
# This file defines environment variables used by this library's tests.
#
# Usage:
# 1) Copy this file to .env.test or .env.local for local testing.
# 2) Fill in the values below. Do not commit real secrets.
#
# macOS (temporary in current shell):
#   export VITE_PROTOPEDIA_API_V2_BASE_URL="https://protopedia.net/v2/api"
#   export VITE_PROTOPEDIA_API_V2_TOKEN="paste-your-token"
#
# Notes:
# - Values are plain strings; no quotes required.
# - Prefer .env.test.local for machine-specific secrets.
# - If not using env files, export variables in your shell.
#

# Base URL for the ProtoPedia API or target API.
# Example (official):
# VITE_PROTOPEDIA_API_V2_BASE_URL=https://protopedia.net/v2/api
VITE_PROTOPEDIA_API_V2_BASE_URL=https://test.example.com/api

# API token for authenticating with the ProtoPedia API.
# Do not commit real tokens.
VITE_PROTOPEDIA_API_V2_TOKEN=token-for-test
